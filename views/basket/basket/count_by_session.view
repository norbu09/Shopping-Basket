[map]
function(doc) {
  if(doc.type == "item"){  
    emit(doc.basket, 1);
  }
}
[reduce]
function(keys, values) {
   return sum(values);
}
[params]
name = count_by_basket
language = javascript
_id = _design/basket
